# Mayan EDMS local environment configuration
# Elasticsearch disabled - using default Django search backend

# Configure Elasticsearch as the search backend (DISABLED)
# MAYAN_SEARCH_BACKEND=mayan.apps.dynamic_search.backends.elasticsearch.ElasticsearchSearchBackend

# Elasticsearch backend arguments (Python dict format with single quotes inside double quotes)
# MAYAN_SEARCH_BACKEND_ARGUMENTS={'client_kwargs':{'basic_auth':('elastic','mayanespassword'),'hosts':['https://elasticsearch:9200'],'verify_certs':False},'indices_namespace':'mayan','search_page_size':100,'point_in_time_keep_alive':'5m'}
